%h5.visible-print= title(Customer, count: 2)
= link_to [:new, @person, :customer] do
  = render partial: 'customers/new'
#customer-list.m-b-lg
  = render partial: 'customers/item', collection: @customers, as: :customer
