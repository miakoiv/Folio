.media[comment]{class: comment.author.deleted? && 'deleted'}
  .pull-left
    = profile_image(comment.author)
  .media-body
    %span.font-bold= comment.author
    %small.text-muted= l(comment.created_at, format: :short)
    .social-content
      = comment.content
