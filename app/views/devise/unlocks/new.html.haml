.text-center.m-b-md
  %h3 Folio
  %small Lähetä uudet avausohjeet
.hpanel
  .panel-body
    = bootstrap_form_for resource, as: resource_name,
        url: unlock_path(resource_name) do |f|
      = f.email_field :email, autofocus: true
      .actions
        = f.submit "Resend unlock instructions", class: 'btn btn-success btn-block'
      = render "devise/shared/links"
