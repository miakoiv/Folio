.hpanel.filter-item
  = link_to customer do
    .panel-body
      %span.label.pull-right{class: "label-#{customer.appearance}"}
        = customer.status
      %h4= l(customer.started_at)
      %small= customer.contacts_to_s
