.profile-picture
  = image_tag '//gravatar.com/avatar/' + Digest::MD5.hexdigest(current_user.email),
      class: 'img-circle m-b'
  .stats-label.text-color
    %span.font-extra-bold.font-uppercase= current_user.full_name
    .dropdown
      %a.dropdown-toggle{data: {toggle: 'dropdown'}}
        %small.text-muted
          Ylläpitäjä
          %b.caret
      %ul.dropdown-menu.animated.flipInX.m-t-xs
        %li
          = link_to destroy_user_session_path, method: :delete do
            %i.fa.fa-fw.fa-sign-out
            Kirjaudu ulos
        %li
          = link_to current_user do
            %i.fa.fa-fw.fa-user
            Oma profiili
