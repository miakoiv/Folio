= form_tag search_path, class: 'navbar-form-custom' do
  .form-group
    = search_field_tag 'search_params[keyword]', @search_params[:keyword],
        class: 'form-control', placeholder: t('.prompt')
