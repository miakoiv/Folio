.color-line
.modal-header
  .pull-right= modal_close_button
  %h4.modal-title= title(Event)
  .small.font-bold
    .label.label-violet= @event.unit
    - if @event.user.present?
      = @event.user
  .small.font-bold= @event.customer.person if @event.customer.present?
= render partial: 'form'
